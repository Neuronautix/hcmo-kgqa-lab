# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# --- Virtual environments --------------------------------------------------
.venv/
venv/
env/
ENV/

# --- Environment / secrets -------------------------------------------------
.env
.env.local
.streamlit/secrets.toml

# --- Test / cache ----------------------------------------------------------
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# --- Generated KG artifacts (keep the directory placeholder) ---------------
kg/generated/*.ttl
!kg/generated/.gitkeep

# --- Fuseki persistent data ------------------------------------------------
fuseki-data/

# --- HCMO sync layer -------------------------------------------------------
# Archived previous vendor snapshots are transient; do NOT commit them.
ontology/vendor/_archive/
# The CURRENT vendored files and the lockfile ARE committed (reproducible
# synced state): ontology/vendor/hcmo/, shapes/vendor/, sparql/vendor/,
# kg/vendor/ and sync/HCMO_SYNC.lock.json are intentionally tracked.

# --- OS / editor -----------------------------------------------------------
.DS_Store
*.swp
.idea/
.vscode/
